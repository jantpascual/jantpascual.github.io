    1  history
    2  ls -ai
    3  cd .git
    4  ls
    5  ls -il
    6  cd ..
    7  which git
    8  git config --global user.name "jantpascual"
    9  git config --global usrr.email "jantpascual@gmail.com"
   10  ~/gitconfig
   11  ~/.gitconfig
   12  cd ~/.ssh
   13  ssh-keygen -t rsa -C jantpascual@gmai.com 
   14  cd ~/.ssh
   15  cd ~
   16  ls -li
   17  ~/.ssh
   18  pwd
   19  cd .ssh
   20  cd /home/curlinux/.ssh/
   21  ls -a
   22  ls -la
   23  pwd
   24  cd..
   25  cd ..
   26  ls -la
   27  cd ..
   28  ls -la
   29  cd etc
   30  cd ssh
   31  ls -la
   32  subl ssh_config
   33  ls -la
   34  cd ~
   35  cd /home/curlinux/.ssh/id_rsa
   36  cd /home/curlinux/.ssh/id_rsa/
   37  ls -la
   38  cd Público
   39  ls -la
   40  cd ..
   41  ssh -T git@github.com
   42  ls -al ~/.ssh
   43  ls -al ~/clave*
   44  ls -al ~ clave*
   45  ls -al ~ clave01
   46  ssh-keygen -t rsa -b 4096 -C "jantpascual@gmail.com"
   47  ls -al ~/.ssh
   48  eval "$(ssh-agent -s)"
   49  ssh-add ~/.ssh/id_rsa
   50  sudo ssh-add ~/.ssh/id_rsa
   51  sudo eval "$(ssh-agent -s)"
   52  eval "$(ssh-agent -s)"
   53  ssh-add ~/.ssh/id_rsa
   54  ls -al ~/.ssh
   55  ssh-add ~/.ssh/clave02
   56  ssh-add ~/.ssh/id_rsa
   57  ssh-add ~/.ssh/clave01
   58  sudo ssh-add ~/.ssh/clave02
   59  ssh-add ~/.ssh/id_rsa
   60  sudo /etc/init.d/ssh start
   61  cd ..
   62  sudo /etc/init.d/ssh start
   63  cd etc
   64  cd init.d
   65  ls -l
   66  sudo ssh start
   67  cd..
   68  cd ~
   69  sudo apt install openssh-server
   70  sudo /etc/init.d/ssh start
   71  ls -al ~/.ssh
   72  ls -al
   73  cd .ssh
   74  ls -al
   75  cd..
   76  cd ~
   77  ssh-keygen -t rsa -b 4096 -C "jantpascual@gmail.com"
   78  ls -al ~/.ssh
   79  cd ~/.ssh
   80  ls -al
   81  cd id_rsa
   82  pwd
   83  cd ~
   84  ls -l
   85  cd pryf
   86  ls  -la
   87  cd jantpascual.github.io
   88  history >>practica4
